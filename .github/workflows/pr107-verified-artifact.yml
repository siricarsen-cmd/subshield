name: PR107 Verified Current Review Fixes

on:
  pull_request:
    paths:
      - .github/workflows/pr107-verified-artifact.yml
      - .github/pr107-current-review-patch.py

permissions:
  contents: write

jobs:
  apply-test-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/orion-parity-grounding-invoice-waiver
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Apply exact current review patch
        run: python .github/pr107-current-review-patch.py

      - name: Align actor-scope regressions with supported waiver grammar
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('lib/analyzer/__tests__/orion-parity-regression.test.mjs')
          text = path.read_text()
          old = "waives the Subcontractor's right to payment."
          new = "waives Subcontractor's right to payment."
          count = text.count(old)
          if count != 2:
              raise SystemExit(f"expected two actor-scope fixtures, found {count}")
          path.write_text(text.replace(old, new))
          PY

      - name: Diagnose Prime-owned payment preservation path
        shell: bash
        run: |
          node --experimental-loader ./lib/analyzer/__tests__/ts-relative-import.loader.mjs --input-type=module <<'NODE'
          import { runDeterministicDetectors } from './lib/analyzer/deterministic.ts';
          import { dedupeFindings, rankFindings } from './lib/analyzer/report.ts';
          import { verifyFindings } from './lib/analyzer/sanity.ts';
          import { normalizeWhitespace } from './lib/analyzer/text.ts';
          const documentText = normalizeWhitespace(`
          2.8 Invoice Requirements
          Failure to submit a complete invoice within 30 calendar days waives Subcontractor's right to payment. Prime Contractor's right to payment for the affected invoice is not waived.
          `);
          const generated = runDeterministicDetectors(documentText);
          const verification = verifyFindings(generated, documentText);
          const ranked = rankFindings(dedupeFindings(verification.verified, documentText));
          console.log(JSON.stringify({
            generated: generated.map(({ regulation, foundText }) => ({ regulation, foundText })),
            dropped: verification.dropped.map(({ finding, reason }) => ({ regulation: finding.regulation, reason })),
            final: [...ranked.primaryTraps, ...ranked.secondaryConcerns].map(({ regulation, foundText }) => ({ regulation, foundText })),
          }, null, 2));
          NODE

      - run: npm run test:accuracy:orion
      - run: npm run test:accuracy
      - run: npx tsc --noEmit
      - name: Build production application
        env:
          VERCEL_ENV: production
          NEXT_PUBLIC_BASE_URL: https://www.subshield.net
          NEXT_PUBLIC_SUPABASE_URL: https://example.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-anon-key
          SUPABASE_URL: https://example.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key
          STRIPE_SECRET_KEY: sk_test_ci_placeholder
          STRIPE_WEBHOOK_SECRET: whsec_ci_placeholder
          NEXT_PUBLIC_STRIPE_PRICE_SINGLE_REVIEW_CYCLE: price_ci_single
          NEXT_PUBLIC_STRIPE_PRICE_ACTIVE_BIDDER_PLAN: price_ci_active
          NEXT_PUBLIC_STRIPE_PRICE_ENTERPRISE_PLAN: price_ci_enterprise
          OPENAI_API_KEY: sk-ci-placeholder
          RESEND_API_KEY: re_ci_placeholder
          CONTACT_FROM_EMAIL: ci@example.com
          CONTACT_TO_EMAIL: ci@example.com
        run: npm run build

      - name: Restore exact six-file scope and push
        shell: bash
        run: |
          rm .github/workflows/pr107-verified-artifact.yml
          rm .github/pr107-current-review-patch.py
          expected=$(printf '%s\n' \
            'lib/analyzer/__fixtures__/orion-parity-regression-fixture.mjs' \
            'lib/analyzer/__tests__/orion-parity-regression.test.mjs' \
            'lib/analyzer/anchors.ts' \
            'lib/analyzer/deterministic.ts' \
            'lib/analyzer/sanity.ts' \
            'package.json')
          actual=$(git diff --name-only 13d4d8f43f24440ebbb0ec77e61736adfd1ad82f | sort)
          test "$actual" = "$expected" || {
            echo 'Unexpected final PR scope:'
            printf '%s\n' "$actual"
            exit 1
          }
          git diff --check
          git config user.name 'siricarsen-cmd'
          git config user.email '287561052+siricarsen-cmd@users.noreply.github.com'
          git add lib/analyzer/deterministic.ts lib/analyzer/__tests__/orion-parity-regression.test.mjs .github/workflows/pr107-verified-artifact.yml .github/pr107-current-review-patch.py
          git diff --cached --check
          git commit -m 'Address PR107 current review edge cases'
          git push origin HEAD:fix/orion-parity-grounding-invoice-waiver
