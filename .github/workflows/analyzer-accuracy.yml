name: Analyzer Accuracy Benchmarks

on:
  pull_request:
    paths:
      - ".github/workflows/analyzer-accuracy.yml"
      - ".github/scripts/orion-align-dispute-verification.py"

permissions:
  contents: write

jobs:
  align-dispute-verification:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out exact PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Apply verified analyzer patch
        run: python .github/scripts/orion-align-dispute-verification.py

      - name: Restore workflow, remove script, and commit
        shell: bash
        run: |
          git fetch origin main
          git checkout origin/main -- .github/workflows/analyzer-accuracy.yml
          rm .github/scripts/orion-align-dispute-verification.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/analyzer/deterministic.ts lib/analyzer/sanity.ts .github/workflows/analyzer-accuracy.yml .github/scripts/orion-align-dispute-verification.py
          git diff --cached --check
          git commit -m "Align dispute finding verification with detector evidence"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
