name: Temporary Authenticated Production Acceptance

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read
  id-token: write

jobs:
  production-acceptance:
    if: github.head_ref == 'test/production-acceptance-20260803' && false
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - run: echo "Acceptance harness update in progress."
